# 项目当前进度与下一步操作

<!-- PROJECT-STATUS:START -->
> 统一进度（2026-09-12，2026-09-12-latest-frontend-backend-alignment）：最新前端合同已对齐，Pages发布中；API版本9继续在线。
> 已完成：TASK-01、TASK-02、TASK-03、TASK-04、TASK-05、TASK-06、TASK-07、TASK-08、TASK-09、TASK-10；进行中：TASK-13、TASK-14；未开始：TASK-12。
> 已跳过：TASK-11（项目负责人（用户）决定）；相应门禁未通过，不得按已完成或待办处理。
> 本次验证：41 个测试文件、509 项通过、0 失败；真实招生发布记录为 51878；已通过：GATE-LOCAL。
> 下一步：完成最新前端Pages发布与公网资产核验；继续TASK-13身份生命周期与TASK-14学生规模、校园网和回滚演练。完整进度及操作见[项目进度](PROJECT_STATUS.md)。历史验证记录不代表当前状态。
<!-- PROJECT-STATUS:END -->

## 当前操作：发布最新前端，后端合同已对齐

负责人要求前端继续作为唯一标准，本轮不改前端源码。已核对 `77b4b09` 及随后交还的全部增量：起航门禁、竖版海报、院校卡大类/小类分组、方向收口冻结和答案按钮排版均为浏览器端逻辑；谈心开场、方向小结和“让溟讲讲就业方向”继续使用现有 `/v1/career/turn` 请求及 `reply/options` 返回，没有新增数据库、路由或字段。线上 API 版本9的健康与就绪检查通过，后端无需重复部署。当前41个测试文件509项全部通过，类型检查和生产构建通过；Pages最新发布正在进行，完成后补记工作流与公网资产核验。视觉验收仍由负责人执行。

## 当前操作：AI 谈心改用 TOTP（API 与 Pages 已上线）

负责人要求参考北辰，将南溟 AI 对话验证从固定字符串换成 TOTP，并交付原始种子供后续发码。API 版本 9 与 Pages `8de1983` 已部署：RFC 6238 SHA-1、30 秒、6 位、允许前后一个窗口；Redis 原子防重放已在线验证，旧固定码从云端环境移除并被线上拒绝。Pages 公网脚本已核对包含 6 位动态码输入、过期和重放提示。原始种子只在负责人最终交付、本机 `private/nanming-totp-secret.txt` 与 SCF 环境变量出现，不写入文档、日志、Git 或源码包。任务与门禁状态不变。

## 2026-09-12 / acceptance-followup：继续发布当前前端

负责人要求继续；核对基线5124e86，上一轮线上为f18f215。本次把其后方向卡片修复、航线图样式及当前talk聊天界面、动效与聊天测试一起纳入快照，不等待在建工作收尾；不覆盖其他任务修改。API源代码未变，保留已经线上核验的版本8和881密文，不重复上传。已有39文件481项本地结果来自其他任务记录，不充当本轮新实测。根目录--package/--check同步后提交推送，等待Pages构建和部署；本轮仍允许测试失败不阻断半成品发布，具体结果以对应Actions运行记录为准。任务与门禁不变，完整视觉/功能验收仍由负责人完成。


## 当前操作：全部快照上云验收（已部署，线上核心链路核验通过）

负责人已明确授权全部当前未提交/未完成前后端发布，不等待前端收尾。Pages测试本轮不阻断验收发布；已按负责人指示从北辰私有交接库读取既有凭据，完成881密文上传，当前API版本9；原环境配置保留，Pages首轮发布成功。线上48人学校查询、guided/open真实千帆speed聊天、TOTP兑换与会话撤销通过，见VALIDATION_RESULT最新补充。最终线上状态以本轮部署完成记录为准，不按旧“未部署”说明推断。



## 最新补充：四班查询码（已上线）

负责人指定的48条身份全部匹配四班；现用身份证后六位，末位X改为0，原48个查询键撤销。其余四班8人经负责人确认保留原码，其余班级不变。48人本机HTTP逐人核验成功，6人覆盖X→0，无重复码，成绩数据库不变。最终类型检查通过，全量36文件454项通过，Python4项通过。任务状态不变，新身份索引与页面说明已随本轮验收快照上线。见[四班查询码](CLASS4_QUERY_CODES.md)。


核对日期：2026-09-12。本轮后端对齐以实际挂载的章节版 `App.tsx` 为准；开始基线 `6e3ec33`，核对过程中其他任务已提交至 `286f4c1`，保留全部并发在建改动。本轮补齐学校汇总响应、AI 请求容量与异常收尾、生产调试隔离、Pages 配置检查；已随本轮验收快照提交、推送及部署。详见 [后端对齐专题](BACKEND_FRONTEND_ALIGNMENT.md)，不能将既有线上健康结果当作新代码线上验收。

## 当前任务

| 任务 | 状态 | 实际交付与边界 |
|---|---|---|
| TASK-01 工程准备 | 完成 | workspaces、锁文件、构建与验证命令，已有 Git 历史 |
| TASK-02 合同验证 | 完成 | 四份冻结 Schema、类型生成及语义断言 |
| TASK-03 招生数据 | 完成 | 数据提供方确认与机器复核完成；51,878 条真实专业记录、90 个分片；发布版本 SC-2026-e48a598a1832 |
| TASK-04 纯规则匹配 | 完成（本地验证范围） | 资格、位次、参考关系、偏好、确定性排序；公共规则由两套前端共享 |
| TASK-05 单人摘要 | 完成 | 固定 accuracy-v1.2 适配；另已接通学校真实工作簿管线，身份权限属于 TASK-13 |
| TASK-06 探索内容 | 完成（最小交付） | 问题、体验卡、专业事实卡、原话证据、方向修订与行动模板 |
| TASK-07 学生流程 | 完成（本地范围） | 实际主入口为章节版 App，按当前前端组织起航、定位、谈心、方向、分数轴与航线图；Worker 区间匹配、图片/文字导出、清除；前端仍在并发收尾 |
| TASK-08 AI/API | 完成基础实现，生产收尾未完成 | 千帆与假上游、两种聊法、思考档位、SSE、额度、原话传递；新请求带发布库方向目录，模型建议在服务端及界面按库内 ID 校验；共享状态运维验收另记 |
| TASK-09 页面视觉 | 完成历史本地阶段 | 既有多视口记录仅对当时版本有效；当前章节版观感由负责人验收，本轮后端不作视觉验收 |
| TASK-10 阶段验收 | 完成本地阶段 | GATE-LOCAL 已通过；历史阶段报告不代表当前线上验收 |
| TASK-11 小范围试用 | 负责人决定跳过 | 不列待办、不算完成；无真实学生理解观察，GATE-PILOT 未通过 |
| TASK-12 数据扩容 | 未开始（后续扩展范围） | 现有全量入库与发布已经属于 TASK-03 完成项；新增覆盖与扩展验收另行推进 |
| TASK-13 本人身份 | 进行中（本地识别已实现） | 姓名＋验证码服务端核对、私有映射、单进程限流已实现；正式绑定生命周期、撤销、审计和共享限流待完成 |
| TASK-14 部署运维 | 进行中 | Pages 工作流、COS 上传、SCF 入口和打包/部署脚本已提交；既有线上 AI 三轮联通另有记录；当前章节前端/词表协议/学校端点已部署供负责人验收，校园网与回滚仍待确认 |

GATE-LOCAL 已通过；GATE-PILOT、GATE-PUBLISH、GATE-APP 仍未通过。公开招生数据包的 PUBLISHED 状态不等于站点上线门禁通过。提交标题中“上线”不能代替线上验收记录。

## 后端对齐本轮验证

学校真实本地核验、SCF 打包产物的 HTTP 聊天/撤销、密文汇总回读通过。后端与 AI 客户端专项 11 文件 168 项通过；最终完整快照 34 文件 439 项通过，类型检查通过。中途并发前端编辑造成的检查失败保留原始日志，以 VALIDATION_RESULT 最新正文为准。线上既有 API/Redis 健康和 Pages CORS、招生指针通过。本轮新代码已部署，脱敏发布证据见verification/acceptance-cloud-api-2026-09-12.json；线上核验单独记录。

## 既有五步壳成果与验证（历史，非当前挂载入口）

- `npm run validate`：26 个测试文件、377 项通过、0 失败，含类型检查，见 [完整日志](verification/npm-validate-journey-2026-09-12.txt)。
- `npm run build:all`：主前端与纸感前端均通过，见 [构建日志](verification/build-journey-2026-09-12.txt)。新主 JS gzip 约 63.12 KB，区间计算独立 Worker 约 19.54 KB；不能把 Worker 当作没有下载成本。
- 新主流程本地 Edge 浏览器：真实招生包、合成 AI/学校 UI 响应，完成选科→目标分区间→两种聊天→自主专业→交集/差集结果；另验近期考试缺线拒绝、学校入口、跨科类考试过滤、旧结果失效、重置焦点循环及 390/320 无溢出。见 [浏览器结果](verification/journey-browser-result.json) 与 [截图目录](verification/journey-screenshots/)。真实数据匹配约 1.4—1.9 秒，仅为本机这几次测量。
- 学校服务端用一条实际发放记录做本地回环核验：正确姓名＋码返回本人，错名配同一码返回拒绝；未输出姓名、验证码或成绩。见 [学校本地核验](verification/school-local-result.json)。这不是全校身份审计，也没有真实学生试用。
- 招生发布仍为 51,878 条、90 分片、2,308 院校；学校仍为 880 人、12 场、7,954 条观察，既有冲突/差异不抹平。本轮不重跑全量招生/成绩解析。
- 既有线上千帆三轮调用、访问码和 CORS 联通见 AI 专题；本轮前端测试未调用真实模型。新的 `direction_catalog` 协议必须与服务端一起发布并重新线上验收。

## 接手后的优先事项

1. 当前主入口为 App/chapters，JourneyApp 为参考；以负责人正在收尾的章节版交互为准，完成全工作区类型检查与测试后再发布。
2. 本人身份：本地姓名＋验证码校验已完成；生产私有数据挂载、身份撤销/审计和共享限流仍需完成。不得公开上传 private/ 或成绩分片。
3. 部署：新前端、方向词表字段和学校端点同步上线前，确认服务端版本、环境、访问权限、手机/校园网与回滚。既有 API 在线不代表新主流程已上线。
4. 并发任务已增加异步共享会话接口及 Redis 源码，13 项 Redis 自动测试计入本次 377 项；是否线上配置、跨实例与运维验收通过，仍须由对应运行记录证明。
5. 模型探索的是有依据的专业方向，不给适配度；专业/院校结果只取发布库，组依据不冒充专业门槛。TASK-11 保持负责人决定跳过。

## 开发与交接命令

在 `nanhang-app/` 下运行：

```powershell
node --version
npm run validate
npm run build:all
npm run web:dev
# 按 AI_QIANFAN_SETUP.md 选择 fake 或 qianfan，再另开终端运行：
npm run api:start
```

在项目根目录运行：

```powershell
py -3.12 tools/sync_project_docs.py --package
py -3.12 tools/sync_project_docs.py --check
```

AI 配置两项千帆凭据齐全时可自动选择真模型；演练应显式选择 fake。密钥不得写进文档、日志或源码包。

## 每次修改、每次提交都要同步

这是负责人于 2026-09-12 明确要求的持续规则：**每次修改和每次提交都必须留下文档记录，文档同步是完成条件。** 不允许仅更新自动摘要而保留正文旧结论。当前任务未完成时也要记录做到哪里、验证了什么和下一步是什么。具体执行顺序、记录模板和接手步骤见 [文档同步与接手规范](DOCUMENTATION_POLICY.md)。
