# 正式前端视觉进度 · apps/web

<!-- PROJECT-STATUS:START -->
> 统一进度（2026-09-12，2026-09-12-latest-frontend-backend-alignment）：最新前端合同已对齐，Pages发布中；API版本9继续在线。
> 已完成：TASK-01、TASK-02、TASK-03、TASK-04、TASK-05、TASK-06、TASK-07、TASK-08、TASK-09、TASK-10；进行中：TASK-13、TASK-14；未开始：TASK-12。
> 已跳过：TASK-11（项目负责人（用户）决定）；相应门禁未通过，不得按已完成或待办处理。
> 本次验证：41 个测试文件、509 项通过、0 失败；真实招生发布记录为 51878；已通过：GATE-LOCAL。
> 下一步：完成最新前端Pages发布与公网资产核验；继续TASK-13身份生命周期与TASK-14学生规模、校园网和回滚演练。完整进度及操作见[项目进度](../../docs/PROJECT_STATUS.md)。历史验证记录不代表当前状态。
<!-- PROJECT-STATUS:END -->

## 当前主前端：五步顺序旅程

2026-09-12 按负责人新流程重构，main.tsx 已挂载 JourneyApp。选科→成绩→AI→自主专业→结果；旧平行七章不再作为主入口。使用浅纸色与深海绿，保留既有海面和品牌图形；动作仅使用 transform/opacity，并支持减少动态效果。

桌面与 390/320 浏览器验证通过；区间计算移入 Worker，结果分页、打印展开完整记录；AI 有等待秒数/停止，错误保留输入。学校端改为姓名+验证码服务端核对，不把前端文件名哈希当作权限。

当前证据见 [浏览器结果](../../docs/verification/journey-browser-result.json) 和 [截图](../../docs/verification/journey-screenshots/)，流程规则见 [ADR-003](../../docs/ADR_003_STUDENT_JOURNEY.md)。

## 起航页窄屏紧凑化（2026-09-12）

负责人指出首页「一条航线，六次靠岸」六张卡与「先定下三件事」面板在手机上排布过松。处理方式：把这两处写死在 JSX 里的间距收进类名（`sail-panel` / `sail-form` / `sail-score` / `sail-act`，桌面数值逐项不变），再在 `style.css` 增加 ≤560px 的起航页专项块收紧内边距、行距与区块间距。390px 实测：单张靠岸卡 150→112px、六张合计 980→720px、面板 560→483px、整页 3105→2662px；1280px 桌面不变。六站说明与选科提示未删减，只压缩留白。提交状态见实施记录，`style.css` 当时与另一路在建改动同文件。

## 以下为旧七章视觉迁移记录（历史）



以原始 `南溟.html`（2113 行）为严格视觉基准，七章流程正式落实到 `apps/web`。原文件保留不动，未另建 demo。

## 状态

- [x] 设计系统落地：`src/style.css` 替换为原 `南溟.html` 17-487 行 CSS，并追加 React 兼容层
- [x] 视觉资源：`src/art.tsx` 移植原 sprite 图标与 4 张插画（sea/lighthouse/compass/harbor）
- [x] 七章分视图：`src/App.tsx` 与 `src/chapters/` 起航 / 定位 / 成绩 / 谈心 / 方向 / 分数轴 / 航线图
- [x] 原导航（航程轨 + 移动底栏）、toast、dialog、滚动揭示、鲲彩蛋
- [x] 分数轴滑块防抖、候选限量渲染、分片缓存
- [x] 航线图 SVG + PNG 导出 + 文字版复制
- [x] `index.html` 字体（Cormorant Garamond / Noto Serif SC）与标题
- [x] `release-loader.ts` 分片缓存 + offeringId → 院校/专业展示映射
- [x] `model.ts` 可选历史分数与 catalog 状态
- [x] `presentation.test.ts` 更新为新设计的真实断言，业务测试保留
- [x] `npm run validate` / `npm run web:build` 通过
- [x] 谈心页对话化：打字指示器、逐字打字机、提问起点按钮、进度条（`src/chat.tsx` + `src/chat.test.ts`）
- [x] 定位页接真实位次与百分位（`scorePosition`，按参考年选表，不插值不外推）
- [x] 分数轴刻度与滑块范围取自官方分段表公布区间（`axisMarks`）
- [x] 院校卡显示工作簿原文的学科门类、专业类、招生数、学费与院校标签
- [x] 方向卡显示本次范围内的真实覆盖（专业条数 / 院校数 / 命中专业类）
- [x] 补上缺失的选科设置界面（首选科目 + 再选科目 + 情景分）

## 与原始 `南溟.html` 的有意差异

原作的示例数据与本项目的证据要求冲突，以下部分**刻意不照搬**，并在界面上说明原因：

- **方向适配度 %**：原作按答案权重算出 `fit%` 并据此排序方向。本项目规定不得由兴趣推断专业
  适合度（`packages/exploration` 的 `PROMPT_BOUNDARY` 第 2 条），因此方向卡只显示真实覆盖
  （多少条专业、多少所院校），不显示适配度，也不按它排序。`chat.test.ts` 有守卫断言防止回退。
- **控制线**：ADR-002 已引入带来源登记的控制线、近五次考试、距线差及线差比例等位参考；换算使用同年分段表，不自动代填匹配情景分。
- **示例院校与位次**：原作的 `SCHOOLS`/`SEG` 是示意数据（含线性插值）。本项目一律使用发布
  包中的真实记录；缺失时显示未知。
- **提分计划**：学校成绩摘要已接入；展示有来源的成绩和知识点，不凭科目满分空间拆分成承诺性提分计划。

## 数据诚实性约定

- 控制线（本科线/特控线）与一分一段若无发布数据：显示“暂未发布”空态，不使用示例数值。
- 历史匹配参考年按发布包可比性选取（当前为 2025）；ADR-002 等位参考使用控制线对应年的分段表。两种用途分别标注年份，不混用。
- 院校/专业展示名由 offeringId 稳定映射原始分片，不用数组下标。
- 学校增强模式已通过本机六位码分片接入成绩章；服务端 academic 接口仍为 403，身份鉴权未完成，不应写成成绩展示尚未实现。
- 真实 directionTags 为空：不伪装适配度、不做方向强筛选。
- 分数线、院校录取结果为示例/未核实时诚实标注；方向结论由学生本人确认，不用 demo fit% 冒充。
- 提问起点（谈心页的例句按钮）只是措辞帮助，不携带权重、不参与计算，也不代表学生已表达。
