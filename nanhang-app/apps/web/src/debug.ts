/**
 * 调试模式（临时）。
 *
 * 现在只做三件事：预填本地演示访问码、在本地假上游不返回结构化建议时注入两条「（调试示例）」建议、
 * 页面上挂一个「调试模式」角标。
 *
 * **不再旁路章节门禁、也不再预置示例探索区间**（负责人 2026-09-13 指出：点了「开始起航」定位就开着，
 * 学生没选入口、没有任何成绩，进去只是一页空的）。门禁按 progress.ts 的真实规则走——
 * 验收时请从起航一步步走；示例区间会让人误以为「定位已完成」，所以也一并删掉了。
 *
 * 本文件只放开关与调试专用常量，不写业务逻辑；所有消费点都必须带 DEBUG_MODE 判断。
 */
export const DEBUG_MODE = import.meta.env.DEV;

/** 调试模式下预填的本地访问码（本地 API 开发档的演示码）。 */
export const DEBUG_ACCESS_CODE = "local-trial-code";
