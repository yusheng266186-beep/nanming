# 项目协作要求

<!-- PROJECT-STATUS:START -->
> 统一进度（2026-09-20，2026-09-20-university-detail-enrichment-82）：完成官方学费队列核查和两库合并后，已对统一库 2,308 所院校批量编排详细档案：每校平均 918 字，保留教育部名录、阳光高考章程入口、学科/学位、招生计划、费用与来源状态；统一库与详细来源表一次性写入，数据库独立验证、68 项 TASK-03 Python 测试通过；未执行线上部署，云端开关仍保持关闭。
> 已完成：TASK-01、TASK-02、TASK-03、TASK-04、TASK-05、TASK-06、TASK-07、TASK-08、TASK-09、TASK-10；进行中：TASK-13、TASK-14；未开始：TASK-12。
> 已跳过：TASK-11（项目负责人（用户）决定）；相应门禁未通过，不得按已完成或待办处理。
> 本次验证：47 个测试文件、554 项通过、0 失败；真实招生发布记录为 51878；已通过：GATE-LOCAL。
> 下一步：统一数据库已生成并作为本地查询入口；后续若招生库或官方学费目录继续更新，应先重建/验证南航规范化招生库，再运行 pipelines/task03/merge_admissions_databases.py 重新生成统一库，不能只替换其中一侧。118 个 institution 实体行仍没有可直接入库的官方明确 CNY/学年金额，继续保持未知；同时按全项目审阅修 P1 与状态生命周期，TASK-13/14 身份与运维收尾不变。完整进度及操作见[项目进度](nanhang-app/docs/PROJECT_STATUS.md)。历史验证记录不代表当前状态。
<!-- PROJECT-STATUS:END -->

负责人明确要求：每次修改和每次提交都必须同步文档，确保随时查看与接手。文档同步属于完成条件，不能只改代码或只刷新顶部摘要。

- 开始前核对 Git 状态、最近提交、项目当前进度、机器状态、实施记录最新条目和验证结果；不要覆盖他人的修改。
- 每个有明确目的的改动在本轮结束/暂停前写入实施记录；未完成也记录当前做到哪里、问题与下一步。代码、配置、数据、部署、测试、工具及纯文档变更都适用。
- 同步当前进度主本与相关专题正文；检查旧结论。对应文档必须与代码在同次提交，不得以后再补。
- 验证区分本次实测、历史结果和未执行，记录命令及脱敏证据；已实现不能冒充已部署或已线上验收。
- 完整工作区根目录执行 `py -3.12 tools/sync_project_docs.py --package` 和 `py -3.12 tools/sync_project_docs.py --check`，通过后才能宣称同步完成。不得手改生成摘要、参考副本、索引、哈希。
- 不记录密钥、学生姓名/成绩、验证码；真实成绩与 private/ 不进分发包。冻结合同不擅改，历史记录不伪造。
- 本规则不授权自动推送或部署。未提交、未部署须如实说明。源码包日期为历史固定文件名，应按进度版本和校验结果判断内容新旧。（2026-09-12：项目负责人已明确授权本工程推送与上云，授权范围与当前线上地址见工程 README「线上部署」。）

- 多 Agent 同时改前端（`apps/web`）时：动手前先读[前端三线并行](nanhang-app/docs/PARALLEL_FRONTEND.md)，按里面的独占/共享文件表认领，不改别人正在改的文件；用 `py -3.12 tools/check_frontend_lanes.py --lane <线号>` 提交前自查越线。
- 界面视觉由负责人自己验收（2026-09-12 指示）：Agent 只做代码与逻辑层面的验证（类型检查、测试、必要的 DOM 断言），不代做浏览器视觉校检，也不把改动写成「已通过视觉验收」。

详细执行规范：[文档同步与接手规范](nanhang-app/docs/DOCUMENTATION_POLICY.md)。主进度、机器状态、实施记录与验证结论位于 `nanhang-app/docs/`。
